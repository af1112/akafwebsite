# 📧 راهنمای سریع Resend - پاسخ به سوال شما

## سوال: باید آدرس فرستنده را به چه ایمیلی تغییر دهم؟

### پاسخ کوتاه:

**برای تست (همین الان):**
- **هیچ تغییری لازم نیست!**
- از همان `onboarding@resend.dev` استفاده کنید
- این آدرس پیش‌فرض Resend است و برای تست کار می‌کند

**برای استفاده واقعی (production):**
- باید دامنه `akafco.com` را در Resend تأیید کنید
- سپس می‌توانید از آدرس‌هایی مثل `noreply@akafco.com` استفاده کنید

---

## 📝 مراحل عملی:

### مرحله 1: دریافت API Key
1. به [resend.com](https://resend.com) بروید
2. وارد حساب کاربری شوید
3. به بخش "API Keys" بروید
4. یک API Key جدید ایجاد کنید
5. آن را کپی کنید

### مرحله 2: تنظیم در پروژه
1. فایل `.env.local` را باز کنید
2. این خط را اضافه کنید:
   ```env
   RESEND_API_KEY=re_xxxxxxxxxxxxx
   ```
   (به جای `re_xxxxxxxxxxxxx`، API Key خود را بگذارید)

3. پکیج را نصب کنید:
   ```bash
   npm install resend
   ```

4. سرور را restart کنید

### مرحله 3: تست
- فرم تماس را پر کنید
- ایمیل باید به `info@akafco.com` ارسال شود
- **فعلاً از `onboarding@resend.dev` استفاده می‌شود** (این درست است!)

---

## 🔧 تغییر آدرس فرستنده (اختیاری - فقط برای production)

### اگر می‌خواهید از آدرس خود استفاده کنید:

**مرحله 1: تأیید دامنه در Resend**
1. در پنل Resend، به "Domains" بروید
2. "Add Domain" را بزنید
3. `akafco.com` را وارد کنید
4. DNS records را طبق دستورالعمل تنظیم کنید
5. منتظر تأیید بمانید

**مرحله 2: تغییر کد**
1. فایل `app/api/contact/route.ts` را باز کنید
2. خط 43 را پیدا کنید:
   ```typescript
   from: 'Contact Form <onboarding@resend.dev>',
   ```
3. به این تغییر دهید:
   ```typescript
   from: 'Contact Form <noreply@akafco.com>',
   ```
   یا
   ```typescript
   from: 'AKAF Contact <contact@akafco.com>',
   ```

---

## ✅ خلاصه:

| وضعیت | چه کاری انجام دهم؟ |
|-------|-------------------|
| **تست** | هیچ تغییری لازم نیست! از `onboarding@resend.dev` استفاده می‌شود |
| **Production** | دامنه را تأیید کنید و آدرس را به `noreply@akafco.com` تغییر دهید |

---

## ❓ سوالات متداول:

**Q: آیا باید حتماً دامنه را تأیید کنم؟**
A: خیر! برای تست و شروع کار، `onboarding@resend.dev` کافی است.

**Q: ایمیل‌ها به کجا ارسال می‌شوند؟**
A: به `info@akafco.com` (این در کد تنظیم شده است)

**Q: آیا می‌توانم از `info@akafco.com` به عنوان فرستنده استفاده کنم؟**
A: بله، اگر دامنه `akafco.com` را در Resend تأیید کرده باشید.

---

**نکته مهم:** برای شروع، فقط API Key را تنظیم کنید و تست کنید. تغییر آدرس فرستنده را برای بعد بگذارید! 🚀




